//textContent is a property of a DOM element that is used to get or set the text content of an element as a string. 
//For example, if we log first.textContent, where first is a reference to the span element in the previous example, it will show the output as:

console.log(first.textContent); // output: Hey I am span



//We can also change the text content of an element, like this:

first.textContent = "Hey I am changed"; // reload the page to see the chang