//The from() method creates a new array from an array-like object. For example,
let str = "Hello";
let arr = Array.from(str);
console.log(arr); 

//In the above code, the from() method takes a string as an argument. 
//The from() method creates a new array from the string. 
//The new array contains the characters of the string.