//Functions are a way to group a block of code together and give it a name, which can then be called/invoked at any point in your code. 
//They are important for keeping your code organized and separating different pieces of logic.

//syntax of a function

/*
function functionName(parameters) {
  // function code
}
  */

function calcAddition(number1, number2) { 
    return number1 + number2; 
}
console.log(calcAddition(6,9));
