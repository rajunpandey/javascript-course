//Parameters are variables used to accept inputs given when the function is called
/*
//1.example shows a function that has one parameter called name:

function greet(name) {
    // function body
  }
    */

  /*

  //2.You can use the parameter inside the print() function as follows:

  function greet(name) {
    console.log(`Hello, ${name}!`);
    console.log("Nice weather today, right?");
  }
  greet("rajun");
  */

  //3.You can have more than one parameter when defining the function

  function greet(name, weather) {
    console.log(`Hello, ${name}!`);
    console.log(`It's ${weather} today, right?`);
  }
  
  greet("rajun", "rainy");

  