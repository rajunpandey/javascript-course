
/*//1.The reverse() method reverses the order of the elements in an array. For example,

let fruits = ["Apple", "Orange", "Banana"];
fruits.reverse();
console.log(fruits); 
*/



/*
//2.lets take another example:
let numbers = [40, 100, 1, 5, 29, 10, 2907];
numbers.reverse();
console.log(numbers); 
*/

//The reverse() method modifies the original array and does not create a new array.
